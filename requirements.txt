# Direct dependencies only; transitive packages are resolved by pip.
# Tested with Python 3.10. Python 3.12 is not supported (Celery 5.2 fails at runtime).

# Core
Django~=4.1.13
django-environ==0.11.2

# Django apps
django-filter==23.5
django-mptt==0.16.0
django-tailwind==3.6.0
django-tinymce==4.0.0
django-widget-tweaks==1.5.0
social-auth-app-django==5.4.2

# Data
psycopg2==2.9.9
redis==5.0.4
Pillow==10.2.0

# Background tasks
celery~=5.2.7
kombu==5.3.5  # celery 5.2 allows kombu <6; keep the version it was run with

# Payments, PDF invoices
braintree==4.26.0
xhtml2pdf==0.2.15

# ASGI (LuckyProject/asgi.py)
channels==4.0.0
daphne==4.1.2

# Used by orders/admin.py
pytz==2022.7.1
